import React from "react";
import ReactDOM from "react-dom";
import ParentForm from "./components/ParentComponent";

ReactDOM.render(
  <div>
    <ParentForm />
  </div>,
  document.getElementById("root")
);
